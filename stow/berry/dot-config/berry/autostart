#!/bin/bash

source ~/.cache/wal/colors.sh

# Set decoration geometry
berryc border_width       2
berryc inner_border_width 1
berryc title_height       22
berryc top_gap            30

# Set decoration colors
berryc focus_color         "$(echo "$color8" | sed -e "s/#//")"
berryc unfocus_color       "$(echo "$color8" | sed -e "s/#//")"
berryc inner_focus_color   "$(echo "$color6" | sed -e "s/#//")"
berryc inner_unfocus_color "$(echo "$color7" | sed -e "s/#//")"
berryc text_focus_color    "$(echo "$color0" | sed -e "s/#//")"
berryc text_unfocus_color  "$(echo "$color8" | sed -e "s/#//")"

# Other options
berryc smart_place "true"
berryc draw_text   "true"
berryc edge_lock   "true"
berryc set_font    "Fira Code-10"

#vim:ft=sh
