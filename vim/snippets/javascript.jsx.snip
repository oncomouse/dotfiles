snippet     react
abbr        react
options     head
	import React from 'react';

	const ${1} = (props) => {
	  const {
	    ${2}
	  } = props;

	  return (
	    <div />
	  );
	}

	export default $1;
